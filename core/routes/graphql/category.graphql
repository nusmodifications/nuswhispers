type Category {
  id: ID! @rename(attribute: "confession_category_id") @globalId
  categoryId: ID! @rename(attribute: "confession_category_id")
  name: String! @rename(attribute: "confession_category")
}